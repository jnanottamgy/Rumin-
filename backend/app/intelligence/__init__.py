"""Financial intelligence: findings, signals and insights computed from what RUMIN stores.

Every result is built from stored records — observations, the knowledge graph, simulation
runs and scenario executions — by a documented method, and carries the chain of evidence
that produced it. Nothing here generates free text, forecasts or recommendations: sentences
are templates filled with computed values, each value with a reference to its record or
calculation. See ``docs/intelligence/``.
"""

INTELLIGENCE_VERSION = "1.0.0"
