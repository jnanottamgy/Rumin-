# The web image is built from the repository root (see Dockerfile): only the web app and
# the API contract its type-check reads.
*
!frontend/
!docs/api/openapi.json
frontend/node_modules
frontend/dist
frontend/coverage
# The launch suite's output holds session cookies of a throwaway run: never in a build.
frontend/e2e-results
**/*.log
